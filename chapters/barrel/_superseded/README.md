These two files belonged to the first, paper-derived draft of the BARReL chapter and are no
longer `\input` from `chapters/barrel.tex`:

- `related-work.tex`  -> folded into `discussion.tex` (\section{Related work})
- `ui-case-analysis.tex` -> rewritten as `interface.tex`

Safe to delete.
