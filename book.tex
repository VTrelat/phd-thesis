% Book version: printed-book jacket (front + back cover artwork), no official
% Université de Lorraine title page, full manuscript, A5 trim.
% Build with `make book` or `latexmk book.tex`; the output is thesis-book.pdf.
\def\ThesisTarget{book}
\input{main.tex}
