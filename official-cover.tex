%% chapters/titlepage.tex
%% Title page inspired by the thesul.cls layout (D. Roegel, LORIA).
%% Adapted for the thesis's scrbook + thesis-style.sty setup.
%%

% Use \clearpage (not titlepage) so scrbook does not insert a \cleardoublepage
% that would push this page to a recto, creating a blank page between the covers.
\clearpage
\begingroup
  \setstretch{1.0}%

  % ── Utility lengths ──────────────────────────────────────────────────────
  \newlength{\TPrule}%
  \setlength{\TPrule}{0.5pt}%

  % ── Symmetric margins for the cover page (inner ≠ outer in body) ─────────
  % \newgeometry calls \clearpage internally; \thispagestyle must come after
  % so it targets the actual cover page, not the intermediate empty one.
  \newgeometry{outer=1in, inner=1in, top=1.2in, bottom=1.2in}%
  \thispagestyle{empty}%

  % ── Page body ────────────────────────────────────────────────────────────
  % \noindent prevents \parindent from shifting the full-width minipage right.
  \noindent
  \begin{minipage}[t][\textheight][s]{\textwidth}

    % ── Header: UL logo + doctoral school + rule ────────────────────────────
    \noindent
    \begin{minipage}[c]{0.45\textwidth}
      \includegraphics[height=18mm]{assets/tulul}%
    \end{minipage}%
    \hfill
    \begin{minipage}[c]{0.50\textwidth}
      \raggedleft
      {\small\bfseries École doctorale IAEM Lorraine}
    \end{minipage}

    \vspace{2mm}
    {\rule{\textwidth}{\TPrule}}

    \vfill

    % ── Title ───────────────────────────────────────────────────────────────
    \begin{center}
      {\fontsize{22}{28}\bfseries\selectfont\ThesisTitle\par}
    \end{center}

    \vfill

    % ── Kind ────────────────────────────────────────────────────────────────
    \begin{center}
      {\fontsize{18}{22}\bfseries\selectfont THÈSE\par}
    \end{center}

    \vspace{4mm}

    % ── Presented / defended ────────────────────────────────────────────────
    \begin{center}
      {\normalsize présentée et soutenue publiquement le \ThesisDate\par}
    \end{center}

    \vspace{4mm}

    % ── Degree ──────────────────────────────────────────────────────────────
    \begin{center}
      {\normalsize pour l'obtention du\par}
      \vspace{2mm}
      {\large\bfseries
        Doctorat de l'Université de Lorraine\par}
      \vspace{1mm}
      {\normalsize (mention informatique)\par}
    \end{center}

    \vspace{4mm}

    % ── Author ──────────────────────────────────────────────────────────────
    \begin{center}
      {\normalsize par\par}
      \vspace{2mm}
      {\large \ThesisAuthor\par}
    \end{center}

    \vfill

    % ── Jury ────────────────────────────────────────────────────────────────
    {\small\bfseries Composition du jury}

    \vspace{2mm}
    \begin{center}
    {\small
    \begin{tabular}{@{}lll@{}}
      \textit{Rapporteurs :}    & Jean-Christophe Filli\^atre  & CNRS, LMF, Université Paris-Saclay \\[2mm]
                                & Michael Leuschel  & Heinrich-Heine-Universität Düsseldorf \\[4mm]
      \textit{Examinateurs :}   & Laura Kov\'acs  & TU Wien \\[2mm]
                                & Xavier Goaoc  & Université de Lorraine, Loria, \textcolor{red}{IUF?} \\[4mm]
      \textit{Encadrants :}     & Stephan Merz     & Inria Nancy \\ [2mm]
                                & Sophie Tourret   & Inria Nancy
    \end{tabular}
    }
    \end{center}

    \vfill

    % ── Footer: rule + lab name ──────────────────────────────────────────────
    {\rule{\textwidth}{\TPrule}}
    \vspace{2mm}
    \begin{center}
      {\small\bfseries
        Laboratoire Lorrain de Recherche en Informatique et ses
        Applications --- UMR 7503\par}
    \end{center}

  \end{minipage}% end page body

  \restoregeometry
\endgroup
\clearpage
